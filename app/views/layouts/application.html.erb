<!DOCTYPE html>
<html>
  <head>
    <title>TechnologyInheritance</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

 <body class="bg-gray-100 text-gray-900 font-sans">
    <header class="bg-white shadow-md py-4 px-6 flex justify-between items-center">
      <h1 class="text-xl font-bold">
        <%= link_to "Skill Link", root_path, class: "text-blue-600 hover:text-blue-800" %>
      </h1>

      <% if user_signed_in? %>
        <div class="text-sm">
          <span class="mr-4">こんにちは、<strong><%= current_user.nick_name %></strong> さん</span>
          <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-600 text-white py-1 px-3 rounded" %>
        </div>
      <% else %>
        <div class="text-sm space-x-2">
          <%= link_to "ログイン", new_user_session_path, class: "text-blue-600 hover:underline" %>
          <%= link_to "新規登録", new_user_registration_path, class: "text-blue-600 hover:underline" %>
        </div>
      <% end %>
    </header>

    <main class="container mx-auto p-6">
      <%= yield %>
    </main>
  </body>
</html>

