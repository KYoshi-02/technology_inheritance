<%= form_with(model: knowhow_post) do |form| %>
  <% if knowhow_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(knowhow_post.errors.count, "error") %> prohibited this knowhow_post from being saved:</h2>

      <ul>
        <% knowhow_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
